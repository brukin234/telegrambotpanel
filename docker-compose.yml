services:
  botpanel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: botpanel
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - botpanel-network

networks:
  botpanel-network:
    driver: bridge

